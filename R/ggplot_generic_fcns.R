# ggplot_generic <- function(data, x, y, z, stack_by, group,
#                            x_limits, y_limits, flip_coord) {
#   gg_chart <- ggplot(data)
#   gg_chart <- get_specifics()
# }
#
# overlay <- function(lo_chart_specs) {
#   chart_geoms <- lapply(lo_chart_specs, function(chart_specs){
#     get_specifics(chart_specs)
#   })
#   ggplot(data) + chart_geoms
#
# }

# get_specifics()
